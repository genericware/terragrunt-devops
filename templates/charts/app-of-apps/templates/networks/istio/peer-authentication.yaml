{{ if or .Values.applications.istio.enabled (eq .Values.network.mode "istio") }}
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: {{ .Values.applications.istio.istiod.namespace }}
  # todo: annotations
spec:
  mtls:
    mode: PERMISSIVE
{{ end }}
